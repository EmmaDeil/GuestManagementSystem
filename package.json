{
  "name": "guest-management-app",
  "version": "1.0.0",
  "description": "Complete Guest Management System with Frontend and Backend",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "start": "concurrently \"npm run server:start\" \"npm run client:start\"",
    "build": "npm run client:build && npm run server:build",
    "build:production": "cd client && npm install && npm run build && cd ../server && npm install && npm run build",
    "server:dev": "cd server && npm run dev",
    "server:start": "cd server && npm start",
    "server:build": "cd server && npm run build",
    "client:dev": "cd client && npm run dev",
    "client:start": "cd client && npm start",
    "client:build": "cd client && npm run build",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "clean": "cd server && rm -rf dist node_modules && cd ../client && rm -rf .next out node_modules"
  },
  "keywords": [
    "guest-management",
    "nodejs",
    "nextjs",
    "mongodb",
    "typescript"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}